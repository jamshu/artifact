odoo-agent-nodejs/
│
├── 📚 Documentation
│   ├── README.md                    # Complete user guide
│   ├── QUICKSTART.md                # 5-minute setup guide
│   ├── EXAMPLES.md                  # Real-world usage examples
│   ├── PROJECT_SUMMARY.md           # Implementation overview
│   └── STRUCTURE.txt                # This file
│
├── ⚙️  Configuration
│   ├── package.json                 # Dependencies & scripts
│   ├── .env.example                 # Environment template
│   ├── .gitignore                   # Git ignore rules
│   └── .env                         # Your config (create this)
│
├── 🚀 Entry Point
│   └── index.js                     # Main CLI entry
│
└── 📦 Source Code (src/)
    │
    ├── 🤖 agent/
    │   ├── odoo-agent.js            # Core agent logic
    │   │   ├── createModule()       # Generate new modules
    │   │   ├── customizeModule()    # Modify existing modules
    │   │   ├── debugIssue()         # Fix Odoo errors
    │   │   └── analyzeCode()        # Code quality analysis
    │   │
    │   └── file-change.js           # File operation representation
    │       ├── FileChange class     # Tracks file modifications
    │       └── Helper methods       # Create/modify/delete
    │
    ├── 💻 cli/
    │   ├── interactive.js           # Menu-driven interface
    │   │   ├── InteractiveCLI       # Main interactive class
    │   │   ├── handleCreateModule() # Module creation flow
    │   │   ├── handleCustomize()    # Customization flow
    │   │   ├── handleDebug()        # Debug flow
    │   │   └── handleSettings()     # Settings management
    │   │
    │   └── commands.js              # Command-line interface
    │       ├── CommandCLI           # Command parser
    │       ├── create command       # Direct module creation
    │       ├── customize command    # Direct customization
    │       ├── debug command        # Direct debugging
    │       └── analyze command      # Direct analysis
    │
    ├── ⚙️  config/
    │   └── config.js                # Configuration manager
    │       ├── Load .env            # Environment variables
    │       ├── Validate settings    # Check required keys
    │       └── Export config        # Global config object
    │
    ├── 🧠 llm/
    │   └── llm-manager.js           # LLM provider manager
    │       ├── LLMManager class     # Provider abstraction
    │       ├── getGeminiLLM()       # Google Gemini setup
    │       ├── getOpenAILLM()       # OpenAI GPT setup
    │       ├── getAnthropicLLM()    # Claude setup
    │       └── invoke()             # Send prompt to LLM
    │
    ├── 📊 parsers/
    │   └── toon-parser.js           # Toon/JSON parser
    │       ├── parse()              # Auto-detect format
    │       ├── parseToon()          # Toon format parser
    │       ├── parseJSON()          # JSON fallback
    │       ├── parseModuleCreation() # Module data parser
    │       ├── parseCustomization() # Customization parser
    │       ├── parseDebugIssue()    # Debug data parser
    │       └── parseCodeAnalysis()  # Analysis parser
    │
    ├── 📝 prompts/
    │   └── templates.js             # Toon-enabled prompts
    │       ├── MODULE_CREATION      # Create module prompt
    │       ├── MODULE_CUSTOMIZATION # Customize prompt
    │       ├── DEBUG_ISSUE          # Debug prompt
    │       ├── CODE_ANALYSIS        # Analysis prompt
    │       ├── TOON_FORMAT_INST     # Toon instructions
    │       └── formatPrompt()       # Template formatter
    │
    └── 🛠️  utils/
        ├── diff-viewer.js           # Colored diff display
        │   ├── showDiff()           # Show file differences
        │   ├── showNewFile()        # Display new file
        │   ├── showDeletedFile()    # Display deletion
        │   └── getDiffStats()       # Calculate changes
        │
        ├── file-manager.js          # File operations
        │   ├── createBackup()       # Backup before modify
        │   ├── createFile()         # Write new file
        │   ├── modifyFile()         # Update existing
        │   ├── deleteFile()         # Remove file
        │   ├── readFile()           # Read content
        │   ├── getModuleStructure() # Analyze module
        │   └── applyFileChanges()   # Batch operations
        │
        └── ui-helpers.js            # Terminal UI utilities
            ├── showBanner()         # Welcome screen
            ├── showChangesSummary() # Changes table
            ├── askForApproval()     # Interactive approval
            ├── showSpinner()        # Loading indicator
            ├── showSuccess()        # Success message
            ├── showError()          # Error message
            ├── showResults()        # Operation results
            └── askChoice()          # User selection

Key Files by Function:
━━━━━━━━━━━━━━━━━━━━━

🎯 Entry Points:
   • index.js              → Main CLI entry (auto-detects mode)

🤖 Core Logic:
   • odoo-agent.js         → All agent operations
   • llm-manager.js        → LLM provider handling
   • toon-parser.js        → Response parsing

💬 User Interaction:
   • interactive.js        → Menu-driven UI
   • commands.js           → Command-line UI
   • ui-helpers.js         → UI components

📝 AI Instructions:
   • templates.js          → Toon-enabled prompts

💾 Data Operations:
   • file-manager.js       → File CRUD operations
   • file-change.js        → Change tracking

Token Efficiency:
━━━━━━━━━━━━━━━━

JSON Response:     15,000 tokens   💸
Toon Response:      7,500 tokens   💰
Savings:           ~50%            ✨

Dependencies:
━━━━━━━━━━━━━

Production:
  • @langchain/* (4 packages)  → LLM integration
  • @toon-format/toon          → Toon parser
  • chalk                      → Colors
  • inquirer                   → Prompts
  • commander                  • CLI framework
  • diff                       → File diffs
  • fs-extra                   → File ops
  • dotenv                     → Config
  • cli-table3                 → Tables

Usage Modes:
━━━━━━━━━━━━

1. Interactive Mode (default)
   $ npm start
   
2. Command Mode
   $ node index.js create <name>
   $ node index.js customize <path>
   $ node index.js debug
   $ node index.js analyze <file>

Quick Start:
━━━━━━━━━━━

1. npm install
2. cp .env.example .env
3. Add API key to .env
4. npm start
5. Create your first module!

